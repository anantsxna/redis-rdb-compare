# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Java CI with Maven

on:
  push:
    branches: [ "main", "anantsxna-patch-1" ]
  pull_request:
    branches: [ "main", "anantsxna-patch-1" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 18
      uses: actions/setup-java@v3
      with:
        java-version: '18'
        distribution: 'corretto'
        cache: maven
    - name: Build with Maven
      run: mvn --batch-mode --update-snapshots -U package --file redis-rdb-compare/pom.xml
    - name: Run LS
      run: ls -R
#     - name: Upload coverage to Codecov
#       uses: codecov/codecov-action@v2
#       with:
#         token: ${{ secrets.CODECOV_TOKEN }}
#         working-directory: /home/runner/work/redis-rdb-compare/redis-rdb-compare/redis-rdb-compare/target/site/jacoco/
#         env_vars: OS,PYTHON
#         fail_ci_if_error: true
#         files: ./jacoco.xml
#         flags: unittests
#         name: codecov-umbrella
#         verbose: true
      
